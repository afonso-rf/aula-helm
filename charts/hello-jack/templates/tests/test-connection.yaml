apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-jack.fullname" . }}-test-connection"
  labels:
    {{- include "hello-jack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-jack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
