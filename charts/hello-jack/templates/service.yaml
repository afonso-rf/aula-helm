apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-jack.fullname" . }}
  labels:
    {{- include "hello-jack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-jack.selectorLabels" . | nindent 4 }}
