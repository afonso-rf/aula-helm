apiVersion: v1
kind: Service
metadata:
  name: {{ include "aula-week3.fullname" . }}
  labels:
    {{- include "aula-week3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aula-week3.selectorLabels" . | nindent 4 }}
