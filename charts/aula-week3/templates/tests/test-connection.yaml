apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aula-week3.fullname" . }}-test-connection"
  labels:
    {{- include "aula-week3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aula-week3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
